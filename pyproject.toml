[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "plato-surprise-detector"
version = "0.1.0"
description = "Surprise detection for PLATO — tracks prediction errors and triggers healing"
requires-python = ">=3.10"
dependencies = ["requests>=2.28"]